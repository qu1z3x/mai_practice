# Самолеты - индексная сортировка

Программа читает рейсы из файла, проверяет данные и печатает таблицу по времени посадки

## Как устроена программа

`parseTime` - проверяет время

`isGoodBoardNumber` - проверяет борт

`parsePlane` - разбирает строку

`loadPlanes` - читает файл

`fillIndexes` - заполняет индексы

`sortIndexesByTime` - сортирует индексы

`printTable` - печатает таблицу

Базовые ошибки тоже обрабатываются: пустой файл, пустая строка, кривое время, плохой борт и битые данные

## Главное

Массив структур не сортируется

Сортируется только индексный массив:

```cpp
int indexes[MAX_PLANES];
```

Вывод идет через индекс:

```cpp
planes[indexes[i]]
```

То есть сортировка идет через индексы, а не через сами записи

## Входные данные

Файл по умолчанию `planes.txt`

Одна строка - один самолет:

```text
13:25 МС-21 Б-3051 НИЖНИЙ_НОВГОРОД
```

Поля:

```text
время марка бортовой_номер пункт_отправления
```

Бортовой номер строго в формате `Б-хххх`

Пункт отправления пишется одним словом

Если в названии города есть пробел, можно использовать `_`

Для проверки есть файлы `planes_positive.txt` и `planes_negative.txt`

## Запуск

Обычно программа берет данные из `planes.txt`

```bash
g++ main.cpp -o main
./main
```

С другим файлом:

```bash
./main my_planes.txt
```

Например:

```bash
./main planes_positive.txt
./main planes_negative.txt
```

## Пример результата

```text
Самолеты по возрастанию времени посадки:

Время | Марка        | Борт         | Пункт отправления
-------------------------------------------------------------
06:30 | BOEING-737   | Б-0737       | КАЗАНЬ
08:10 | ТУ-204       | Б-4021       | НОВОСИБИРСК
09:40 | ИЛ-96        | Б-9601       | СОЧИ
13:25 | МС-21        | Б-3051       | НИЖНИЙ_НОВГОРОД
```
